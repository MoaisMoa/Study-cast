<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.younghee.dao.UserMapper">
    <resultMap id="UserResultMap" type="com.younghee.dto.UserDTO">
        <id property="userUuid" column="user_uuid"/>
        <result property="userEmail" column="user_email"/>
        <result property="userPassword" column="user_password"/>
        <result property="userName" column="user_name"/>
        <result property="userProfileImage" column="user_profile_image"/>
        <result property="userStatus" column="user_status"/>
        <result property="createdAt" column="create_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deletedAt" column="deleted_at"/>
    </resultMap>

    <!-- 이메일로 회원 조회 -->
    <select id="findByEmail" resultMap="UserResultMap">
        SELECT
            user_uuid,
            user_email,
            user_password,
            user_name,
            user_profile_image,
            user_status,
            created_at,
            updated_at,
            deleted_at
        FROM users
        WHERE user_email = #{userEmail}
    </select>
</mapper>